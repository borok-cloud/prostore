// import { PrismaClient } from './generated/prisma';

// const globalForPrisma = global as unknown as { prismaGlobal: PrismaClient | undefined };

// export const prismaGlobal = globalForPrisma.prismaGlobal ?? new PrismaClient();

// if (process.env.NODE_ENV !== 'production') globalForPrisma.prismaGlobal = prismaGlobal;